// 불리언(Boolean)
// true(참) , false(거짓)
let bool1 = true;
let bool2 = false;

console.log(bool1); //true
console.log(typeof bool1); // boolean

//undefined
let x; // undefined

// null: 값이 존재하지 않음을 '명시적'으로 나타내는 방법
// undefined 와는 다름  undefined 는 값이 없음

//object(객체) : key-value pair
let person = {
    name: 'choi',
    age: 20,
    isMarried: true,
};

//array (배열)
// 여러개의 데이터를 순서대로 저장하는 타입
let number = [1,2,3,4,5];
// 배열의 순서는 0번부터시작.