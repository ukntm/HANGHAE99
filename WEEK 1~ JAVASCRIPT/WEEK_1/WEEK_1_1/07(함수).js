// 함수 = function(기능)
// input, output

// 1. 함수 선언ㅁ쿤
// 두개의 숫자를 입력 받아서 덧셈을 한후 내보내는 함수
function add(x,y) {
    return x+y;
}

// 2. 함수 표현식
let add2 = function(x,y) {
    return x+y;
};

// 함수를 호출한다(=사용한다)
// 함수명() -> add(입력값)
console.log(add(2,3)) = 5

// 함수의 input => 매개변수
// output : return 문 뒤에 오는값 : 반환값